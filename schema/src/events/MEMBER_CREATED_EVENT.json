{
  "properties": {
    "id": "$uuid",
    "login": "$string",
    "password": "$string",
    "name": "$string",
    "notifyEmail": "$email",
    "activated": "$date"
  },
  "required": ["id", "login", "password", "name", "notifyEmail"]
}
